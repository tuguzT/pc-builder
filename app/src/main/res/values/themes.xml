<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PCBuilder.Base" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryVariant">@color/primaryLightColor</item>
        <item name="colorOnPrimary">@color/primaryTextColor</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondaryColor</item>
        <item name="colorSecondaryVariant">@color/secondaryLightColor</item>
        <item name="colorOnSecondary">@color/secondaryTextColor</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="colorError">@color/errorColor</item>
        <item name="bottomNavigationStyle">@style/Widget.MaterialComponents.BottomNavigationView.Colored</item>
        <item name="alertDialogTheme">@style/Theme.PCBuilder.AlertDialog</item>
    </style>

    <!-- App theme for alert dialog. -->
    <style name="Theme.PCBuilder.AlertDialog" parent="ThemeOverlay.MaterialComponents.Dialog.Alert">
        <item name="buttonBarButtonStyle">@style/Theme.PCBuilder.AlertDialog.ButtonStyle</item>
    </style>

    <!-- App theme for alert dialog button. -->
    <style name="Theme.PCBuilder.AlertDialog.ButtonStyle"
        parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:textColor">?attr/colorSecondary</item>
        <item name="rippleColor">?attr/colorButtonNormal</item>
    </style>

    <!-- App theme for rounded ImageView. -->
    <style name="Theme.PCBuilder.RoundImageView">
        <item name="cornerSize">50%</item>
    </style>

    <!-- Light application theme. -->
    <style name="Theme.PCBuilder" parent="Theme.PCBuilder.Base" />
</resources>
